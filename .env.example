# Base URL of the Estante Viva back-end API.
# Copy this file to `.env.local` for local development, and set the same
# variable in the Cloudflare Pages project settings for production builds.
VITE_API_BASE_URL=https://estante-viva-api.onrender.com
